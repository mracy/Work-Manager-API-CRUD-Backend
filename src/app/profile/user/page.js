

// Profile component (assuming it's in a separate file named Profile.js)
export default function UserProfile() {
    return (
      <div>
        <h1>This is user profile route</h1>
      </div>
    );
  }
"use client"

export default function pageError({
    error,
    reset
}) {
    return (
        <div>
            <h1>Error is coming in about route</h1>
            <p>

                The provided code defines a React component named ProfileLayout that takes a single prop named children. This prop is expected to contain the content that will be rendered within the layout.
            </p>


        </div>
    );
}